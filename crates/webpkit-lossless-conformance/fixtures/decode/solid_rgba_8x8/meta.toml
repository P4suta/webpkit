# Decode conformance fixture: webpkit_lossless::decode(input.webp) must equal
# expected.rgba byte-for-byte. expected.rgba is produced by libwebp `dwebp -pam`
# (header stripped); input.webp is a hand-crafted VP8L stream regenerated by the
# `emit_fixtures` test. Never hand-edit the goldens.
feature = "literal"
level = "must"
note = "8x8 solid colour via 1-symbol Huffman codes; no transform, cache, or meta-huffman"
