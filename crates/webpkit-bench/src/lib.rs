//! Benchmark support crate for `webpkit`.
//!
//! The benchmarks themselves live under `benches/`; this crate exists so the
//! criterion harnesses have a stable workspace member to attach to.
#![forbid(unsafe_code)]
